# =============================================================================
# Decentralized Health Database - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# SECURITY: Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# HashiCorp Vault Configuration
# Docs: https://developer.hashicorp.com/vault/docs/get-started/developer-qs
# -----------------------------------------------------------------------------
# Dev mode root token - used for local development only
# In production, use proper Vault authentication (AppRole, Kubernetes, etc.)
VAULT_DEV_ROOT_TOKEN_ID=dev-root-token

# Vault address and token for backend services
# VAULT_TOKEN takes precedence over VAULT_DEV_ROOT_TOKEN_ID in backend code
VAULT_ADDR=http://127.0.0.1:8200
VAULT_TOKEN=dev-root-token

# -----------------------------------------------------------------------------
# Web3.Storage Configuration
# Docs: https://web3.storage/docs/
# Get your token at: https://web3.storage/tokens/
# -----------------------------------------------------------------------------
# API token for pinning files to IPFS via web3.storage
# Leave empty for local IPFS-only mode
WEB3_STORAGE_TOKEN=

# -----------------------------------------------------------------------------
# Hyperledger Fabric Configuration
# Docs: https://hyperledger-fabric.readthedocs.io/
# Samples: https://github.com/hyperledger/fabric-samples
# -----------------------------------------------------------------------------
# Path to fabric-samples directory (created by start-fabric.sh)
FABRIC_PATH=./fabric-samples
FABRIC_CHANNEL_NAME=mychannel
FABRIC_CHAINCODE_NAME=consent

# -----------------------------------------------------------------------------
# Backend Service Configuration
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=4000
LOG_LEVEL=debug

# -----------------------------------------------------------------------------
# Frontend Service Configuration
# -----------------------------------------------------------------------------
# For Vite dev server - use the backend URL directly
VITE_API_URL=http://localhost:4000

# -----------------------------------------------------------------------------
# IPFS Configuration
# Docs: https://docs.ipfs.tech/
# -----------------------------------------------------------------------------
IPFS_API=http://127.0.0.1:5001
IPFS_GATEWAY=http://127.0.0.1:8080

# -----------------------------------------------------------------------------
# pyUmbral Service Configuration
# Docs: https://pyumbral.readthedocs.io/
# -----------------------------------------------------------------------------
PYUMBRAL_SERVICE_URL=http://127.0.0.1:8000
